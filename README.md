# ppm-image-processor
A PPM image processor implemented twice: once in C++ using an object-oriented approach, once in Python using a procedural approach, to compare how each paradigm handles the same problem. Supports grayscale conversion, color inversion, horizontal flip, and 8-bit normalization, with a written analysis of paradigm tradeoffs.
